import React from 'react';
import './NoScene.scss';

export default class NoScene extends React.Component {
  render() {
    return (
      <div
        className='h5p-no-scene'
      >"No scene" placeholder</div>
    );
  }
}